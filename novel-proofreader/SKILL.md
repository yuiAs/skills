---
name: novel-proofreader
description: Japanese novel and light novel manuscript proofreading and typesetting adjustment. Use this skill whenever the user wants to check, proofread, refine, or adjust the typesetting of a Japanese novel, light novel, or fiction manuscript — including any task mentioning 校正, 推敲, 版組, 原稿チェック, 小説の見直し, or files clearly identifiable as fiction manuscripts (chapter files, scene files, etc.). Triggers also include requests to fix punctuation, normalize ellipses/dashes, remove web-novel-style blank lines, check for redundant expressions, point of view drift, character voice inconsistency, or notation inconsistencies in fiction. Use this skill even when the user does not explicitly say "skill" but is clearly working on improving a Japanese fiction manuscript.
---

# Novel Proofreader

日本語の小説・ライトノベル原稿を対象とした、版組調整と校正・推敲を行うスキルです。

## このスキルの役割

商業出版の慣例に沿った版組への自動調整と、文章面の問題点の指摘・改善案提示を行います。創作物のため、自動で書き換えるのは形式面（版組）に限定し、内容面（校正・推敲）は必ず作者の判断を経るための指摘形式に留めます。

## 禁止事項・必須事項（必読）

### スクリプトによる一括置換の禁止

Python・sed・awk・perl・Node.js・PowerShell の `-replace` など、**スクリプトやシェルコマンドによる検索置換・一括変換は一切禁止**。Bash / PowerShell ツール経由の置換処理も同様に禁止する。「件数が多くて効率的だから」は禁止を解除する理由にならない。

**理由**: 版組ルールには「会話文（鉤括弧内）は変更しない」「区切り記号の前後は保持」「長音符とダッシュの文脈判別」など、正規表現では表現できない文脈依存の例外が多数ある。一括置換は必ず誤変換を混入させ、創作物を破壊する。

修正は必ず以下の手順で行う:

1. Grep / Read で対象箇所を特定する（**検出に Grep を使うのは許可**。禁止されるのは置換の実行のみ）
2. 各箇所を文脈ごと読み、ルールの例外に該当しないか判断する
3. Edit ツール、または自分で修正後の全文を組み立てた Write で書き換える

### ファイル出力の必須化

このスキルの成果物は **ディスク上の出力ファイル** である。会話内での報告や指摘の列挙は、出力ファイルの代わりにならない。

- 版組修正を実行したら、必ず `.typeset` ファイルを Write で出力する
- 校正指摘を実行したら、必ず指摘ファイルを Write で出力する
- 指摘・修正が **0件でも**、その旨を記載した出力ファイルを生成する（出力スキップの理由にしない）
- 各出力後、Read でファイルの存在と内容を確認するまで「完了」と報告してはならない

## 実行モード

デフォルトでは「版組修正 + 校正指摘」の両方を実行します。ユーザーの指示に応じて以下のように切り替えます：

- **両方**（デフォルト）: 版組修正ファイルと校正指摘ファイルの両方を生成
- **版組のみ**: ユーザーが「版組だけ」「typesetting only」等を指示した場合
- **校正のみ**: ユーザーが「校正だけ」「proofreading only」等を指示した場合

版組修正と校正指摘の両方を行う場合は、**必ず** 版組修正を先に実行し、校正指摘は版組修正後のファイルに対して実行します。

## 入出力

- **入力**: 原稿ファイル（テキスト形式、Markdown等。ファイル単位での処理）
- **版組修正の出力**: 元ファイルと同じディレクトリに `<元ファイル名>.typeset.<拡張子>` として別ファイル出力（元ファイルは保持）。ユーザーが出力先を指定した場合はそちらに従う
- **校正指摘の出力**: ユーザーが指定したパス。指定がなければ元ファイルと同じディレクトリに `<元ファイル名>.proofread.md` として出力する（出力先が決まらないことを理由に出力を省略してはならない）

## 作業開始時の確認

スキル起動時、以下を確認します：

1. **対象ファイル**: 処理する原稿ファイルのパス
2. **実行モード**: 両方／版組のみ／校正のみ
3. **校正指摘の出力先**: 両方または校正のみの場合（未回答・未指定ならデフォルトの `<元ファイル名>.proofread.md` を使い、確認のために処理を止めない）
4. **作品固有ルールの有無**: キャラ名表記、用語集、文体方針などをまとめたファイル、または直接の指示。なければ「特になし」で進める

ユーザーのメッセージから1〜4が全て推定できる場合は、確認を省略して処理に入ってよい。

---

## 版組修正（自動修正）

商業出版の慣例に沿って修正します。詳細なルールと検出パターンは `references/typesetting-rules.md` を参照してください。

> **重要**: 版組修正は形式面の機械的修正であり、「作者の意図」を理由にルールをスキップしてはならない。版組ルールに定義された修正項目は **検出→修正→検証** の手順で必ず実行する。作者の意図による例外は、ユーザーが明示的に指示した場合のみ認める。

### 修正項目と実行手順

版組修正は以下の **4カテゴリ** を、カテゴリごとに **検出→修正→件数記録** の順で実行する。「該当なし」と判断する前に必ず検出を行うこと。

#### 1. 記号類の正規化

**検出方法** (Grep ツールで各パターンを検索):
- 三点リーダー: `\.\.\.` (ASCII 3連), `…(?!…)` (単独の …)
- ダッシュ: `–` (en dash), `―` (horizontal bar), `--` (hyphen 2連)
- 感嘆符・疑問符: `[！？!?][^　」』\n]` (直後に全角スペース・閉じ括弧・改行がない箇所)

**修正内容**:
- 三点リーダー: `…` 単独や `...` → `……`（2連）に統一
- ダッシュ: En dash `–` や hyphen `--` → Em dash `——` に変換
- Em dash の連数: 行頭 → `——`（2連）必須、文中 → `—`（単独）も許容
- 感嘆符・疑問符: 直後に文が続く場合、全角スペース1つを挿入（文末・閉じ括弧前は不要）

#### 2. 段落・空行の整理

**検出方法**:
- 対象ファイルの総行数と、空行を除いたテキスト行数を比較する。差が大きい場合は Web 小説形式の可能性が高い
- 簡易判定: ファイル冒頭の10段落を Read で確認し、**全ての段落間に空行が1行ずつ入っているか** を目視で確認する

**修正内容**:
- Web小説形式の「全段落間に1行空け」を解消し、通常の改行のみの段落区切りに変換する
- **保持する空行**: 2行以上連続する空行、`---` や `◇` `◆` `＊` `* * *` などの区切り記号を含む行の前後（シーン転換の意図とみなす）
- 行頭の字下げ: 段落冒頭は全角スペース1字下げを基本（鉤括弧 `「` `『` やダッシュ `——` で始まる段落は字下げしない）

#### 3. 句読点の調整

**検出方法**:
- 地の文（鉤括弧外）の全文を対象に、句点 `。` から次の句点までを1文として読点 `、` の数を数える
- **読点が3つ以上ある文は全て要修正候補**として拾う（並列列挙のみで構成された文を除く）
- プロジェクトの CLAUDE.md に読点ルールが定義されている場合はそれを優先する

**修正内容**:
- `references/typesetting-rules.md` セクション3の「読点を打つ4条件」に照らし、どの条件にも当てはまらない読点を削除する
- 読点を3つ以上必要とする文は、削除だけで済ませず**文の分割または係り受けの組み替え**で対処する（同セクション3.3）
- **会話文（鉤括弧内）は原則変更しない**（キャラクターの個性表現）
- 削除・組み替えの前後で文意が変わらないことを各箇所で確認する

#### 4. 長文の分割

**検出方法**:
- 100文字を超える文を目視で確認する（句点 `。` から次の句点までの文字数）

**修正内容**:
- 100文字超かつ複数の節を含む文を、自然な箇所（接続助詞、テ形接続）で分割する
- 著しく長い文のみが対象。文体として長文を選んでいる可能性も考慮し、保守的に適用する

### 版組修正の出力フォーマット

修正後の原稿を出力ファイルに保存し、ユーザーに以下を報告する：

```
版組修正完了: <出力ファイルパス>

修正内訳:
- 三点リーダー正規化: N箇所
- ダッシュ正規化: N箇所
- 感嘆符・疑問符後スペース: N箇所
- Web形式空行削除: N箇所
- 行頭字下げ調整: N箇所
- 過剰読点の調整: N箇所
- 長文分割: N箇所

保持された区切り（シーン転換等）: N箇所
```

---

## 校正・推敲（指摘＋改善案）

文章面の問題は自動修正せず、指摘と改善案の提示に留めます。詳細な観点とパターンは `references/proofreading-checks.md` を参照してください。

### 指摘の観点

**主要な指摘対象（積極的に拾う）**:
- 冗長な表現
- 説明不足（読者が状況を把握できない箇所）
- 主語と述語の関係が不明瞭
- 漢字・単語の誤用の可能性
- メタ表現の混入（ファイル名、「第1話」「プロローグ.md」など作品管理上の概念が本文中で言及されている）
- 表記揺れ（キャラクター名、地名、用語、固有名詞）
- 視点（POV）のブレ
- 時制の不自然な混在
- キャラクターの口調・一人称の揺れ
- 修飾語と被修飾語の距離が離れすぎている
- 「の」の連鎖（3つ以上連続）
- 受動態の濫用、二重否定

**控えめに指摘（よほど気になる時のみ）**:
- 文章のリズム（同じ語尾の連続など）
- 構造的問題（段落構成、シーン構成など）

**最低限に留める**:
- 指示語（あれ・これ・それ）の指摘 — ファイル単位の処理ではコンテキストが限定的なため

### 校正指摘の出力フォーマット

ユーザー指定パスにMarkdown形式で以下のように出力します：

```markdown
# 校正指摘: <原稿ファイル名>

実行日時: YYYY-MM-DD HH:MM
対象: <ファイルパス>

---

## 1. [カテゴリ名]（例: 冗長な表現）

### 1-1. <該当箇所の位置情報（行番号や見出し）>

**該当箇所**:
> 引用文

**指摘**: どこがどう問題か、簡潔に。

**改善案**:
> 改善後の文章

---

## 2. [次のカテゴリ]
...
```

カテゴリごとにまとめ、指摘の重要度や量に応じて並び順を調整します（重要なもの・修正コストが低いものを上位に）。

---

## 作品固有ルールの扱い

ユーザーから提供された作品固有のルール（キャラ名一覧、用語集、文体方針など）は、校正指摘の参照基準として利用します：

- キャラクター名・地名・用語の表記揺れ検出の基準
- 文体方針（語尾、一人称、口調など）の遵守チェック
- 指定された禁則表現の検出
- プロジェクトの `expression-rules.md` など、作品固有の表現ルールファイルがあれば読み込んで適用する

ルールが提供されない場合は、本文内での自己整合性のみをチェックします（例: 同じ名前が「アリス」と「ありす」で混在していないか）。

---

## 処理の流れ

### ステップ 1: 確認
対象ファイル、モード、出力先、作品ルールの有無を確認する。ユーザーのメッセージから全て推定できる場合はスキップ可。

### ステップ 2: 読み込み
- 原稿ファイルを読み込む
- 作品固有ルールがある場合はそれも読み込む
- `references/typesetting-rules.md` と `references/proofreading-checks.md` を読み込む

### ステップ 3: 版組修正の事前検出（モードに応じて）

**このステップは省略不可。** 修正対象の4カテゴリそれぞれについて、実際にファイル内を調べて問題箇所の有無と規模を把握する。

各カテゴリの検出方法は「版組修正 > 修正項目と実行手順」に記載。

検出結果の例:
```
事前検出結果:
- 記号類: 三点リーダー異常 0件, ダッシュ異常 0件, 感嘆符後スペース 0件
- 段落・空行: Web形式空行あり (推定 200行), 字下げ異常 0件
- 句読点: 地の文の読点密度 平均5.2個/文 (目安の1〜3個を超過)
- 長文: 100文字超の文 2件
```

### ステップ 4: 版組修正の実行（モードに応じて）

事前検出の結果に基づき、検出された問題を修正する。修正は以下の順序で適用する:

1. 区切り記号・複数行空行の保護（変更されないようマーク）
2. 三点リーダーの正規化
3. ダッシュの正規化と連数調整
4. Web形式の段落間1行空けを削除
5. 行頭字下げの調整
6. 感嘆符・疑問符後のスペース挿入
7. 過剰な読点の間引き
8. 長文の分割

修正は Edit / Write ツールのみで行う（スクリプトによる一括置換は禁止。「禁止事項・必須事項」参照）。修正後のファイルを出力先に書き出す。

### ステップ 5: 版組修正の出力検証（省略不可）

出力ファイルの **冒頭20行程度を Read で読み**、以下を確認する:

- [ ] 出力ファイルが実際にディスク上に存在するか（Read が成功すること自体が確認になる）
- [ ] 段落間の空行が削除されているか（Web形式空行があった場合）
- [ ] `---` 前後の空行が保持されているか
- [ ] 読点の間引きが反映されているか（修正前後を比較）
- [ ] タイトル行や字下げが崩れていないか

検証に失敗した場合は、該当箇所を修正して再出力する。

### ステップ 6: 修正サマリの報告

修正内訳を件数付きでユーザーに報告する。フォーマットは「版組修正の出力フォーマット」に従う。

### ステップ 7: 校正指摘の実行（モードに応じて）

`references/proofreading-checks.md` を参照しながら問題箇所を抽出し、カテゴリ別に整理して **必ず指摘ファイルに Write で出力する**。指摘が0件でもその旨を記したファイルを出力する。

会話内での概要報告は、ファイル出力が完了した **後** に行う（会話内報告でファイル出力を代替してはならない）。

### ステップ 8: 完了確認（省略不可）

完了を報告する前に、以下を確認する:

- [ ] 版組修正ファイル（`.typeset`）が存在し、検証済みか（版組モード実行時）
- [ ] 校正指摘ファイルが存在し、Read で内容を確認したか（校正モード実行時）
- [ ] 修正・指摘の件数をユーザーに報告したか

いずれかの出力ファイルが欠けている場合、タスクは未完了である。このチェックを通過するまで完了報告をしてはならない。

---

## 複数ファイルの処理

複数ファイルを処理する場合は、Agent ツールで並列処理してよい。各エージェントには以下を渡す:

- 担当ファイルのパス
- 版組修正ルールの要約（読点間引きルール、空行削除ルール等）
- 出力先パス
- 会話文は変更しない旨の注意
- **スクリプト・コマンドによる一括置換は禁止、修正は Edit / Write ツールのみで行う旨**
- **出力ファイルの生成は必須で、生成後に Read で存在確認する旨**

エージェントの完了後、各出力ファイルが実在することを確認した上で、出力ファイルの検証（ステップ5）を必ず行う。エージェントが「完了した」と報告していても、ファイルが存在しなければ未完了として該当ファイルを処理し直す。

---

## 注意事項

### 版組修正について
- 版組修正は **機械的な形式修正** であり、「作者の意図」「文体の特徴」を理由にルールの適用をスキップしない
- 「該当なし」と判断する前に、必ず Grep やファイル読み込みによる検出を行う
- 検出の結果、本当に該当箇所がない場合のみ「0件」と報告する
- 会話文（鉤括弧内）は版組修正の対象外（読点間引き等を適用しない）
- 修正の適用は Edit / Write ツールのみ。Python 等のスクリプトによる一括置換は、件数が多くても禁止（「禁止事項・必須事項」参照）

### 出力ファイルについて
- 版組修正ファイル・校正指摘ファイルの出力は省略不可。修正・指摘が0件でも出力する
- 出力ファイルを Read で確認するまで完了報告をしない（ステップ8参照）

### 校正・推敲について
- **創作物の尊重**: 文体、リズム、独特な表現は作者の意図である可能性が高いため、明らかな誤りや一貫性の欠如以外は指摘しない
- **過剰指摘の回避**: 全ての箇所を指摘するのではなく、改善効果の高いものに絞る
- **作中の世界観優先**: ファンタジー作品等で意図的に造語や古めかしい表現が使われている場合、それを誤用として扱わない
